import UserSettings from "../../components/UserSettings"
import Layout from "../../layouts/Layout"

export default function Settings() {
  return (
    <Layout title="Where to watch | Settings">
      <h1>Settings</h1>
      <UserSettings />
    </Layout>
  )
}
